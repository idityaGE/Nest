kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  kubeadmConfigPatches:
  - |
    kind: InitConfiguration
    nodeRegistration:
      kubeletExtraArgs:
        node-labels: "ingress-ready=true"
  extraPortMappings:
  - containerPort: 80
    hostPort: 80
    protocol: TCP
  - containerPort: 443
    hostPort: 443
    protocol: TCP
  # resources:
  #   limits:
  #     cpu: "4"
  #     memory: "5Gi"



# echo "127.0.0.1 nest.local" | sudo tee -a /etc/hosts

# Frontend: http://nest.local/
# Backend API: http://nest.local/api/
# GraphQL endpoint: http://nest.local/graphql/
